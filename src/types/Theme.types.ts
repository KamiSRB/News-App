export interface Theme {
  color: {
    hoverLink: string;
    border: string;
    disabled: string;
    active: string;
    activeLink: string;
    nav: string;
    hoverNav: string;
    background: string;
  };

  fontFamily: {
    serif: string;
    sansSerif: string;
  };
}
